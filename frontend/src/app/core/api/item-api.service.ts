import { Injectable } from '@angular/core';

/**
 * Item API service — implements the item operations from API-CONTRACT.md.
 * This is the ONLY place that touches the backend for item-related operations.
 * TODO: implement with Firebase backend (see backend/BACKEND.md).
 */
@Injectable({ providedIn: 'root' })
export class ItemApiService {
  // TODO: implement API-CONTRACT.md §5 operations
}
