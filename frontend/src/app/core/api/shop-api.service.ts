import { Injectable } from '@angular/core';

/**
 * Shop API service — implements the shop operations from API-CONTRACT.md.
 * This is the ONLY place that touches the backend for shop-related operations.
 * TODO: implement with Firebase backend (see backend/BACKEND.md).
 */
@Injectable({ providedIn: 'root' })
export class ShopApiService {
  // TODO: implement API-CONTRACT.md §5 operations
}
