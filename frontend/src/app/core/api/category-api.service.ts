import { Injectable } from '@angular/core';

/**
 * Category API service — implements the category operations from API-CONTRACT.md.
 * This is the ONLY place that touches the backend for category-related operations.
 * TODO: implement with Firebase backend (see backend/BACKEND.md).
 */
@Injectable({ providedIn: 'root' })
export class CategoryApiService {
  // TODO: implement API-CONTRACT.md §5 operations
}
