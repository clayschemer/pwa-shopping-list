import { Injectable } from '@angular/core';

/**
 * Account API service — implements the account operations from API-CONTRACT.md.
 * This is the ONLY place that touches the backend for account-related operations.
 * TODO: implement with Firebase backend (see backend/BACKEND.md).
 */
@Injectable({ providedIn: 'root' })
export class AccountApiService {
  // TODO: implement API-CONTRACT.md §5 operations
}
