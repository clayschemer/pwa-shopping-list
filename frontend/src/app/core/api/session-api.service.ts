import { Injectable } from '@angular/core';

/**
 * Session API service — implements the session operations from API-CONTRACT.md.
 * This is the ONLY place that touches the backend for session-related operations.
 * TODO: implement with Firebase backend (see backend/BACKEND.md).
 */
@Injectable({ providedIn: 'root' })
export class SessionApiService {
  // TODO: implement API-CONTRACT.md §5 operations
}
