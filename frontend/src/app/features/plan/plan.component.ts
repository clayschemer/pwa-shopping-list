import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-plan',
  standalone: true,
  template: '<p>Plan mode — coming soon</p>',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class PlanComponent {}
