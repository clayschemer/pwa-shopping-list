// Global test setup.
// Note: Angular TestBed initTestEnvironment() is called per-spec via
// src/testing/init-testbed.ts, because Vitest's vmThreads/vmForks pools
// do not guarantee that setupFiles share a module instance with spec files.
