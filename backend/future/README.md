# Future Backend Placeholder

When the Firebase backend is replaced, only `frontend/src/app/core/api/` changes.

Candidate: Java or Go + PostgreSQL with a true SSE endpoint.
The SSE contract must satisfy the `EntityChangeBatch<T>` stream model in `API-CONTRACT.md §4`.

Decisions and notes for the future implementation will be tracked here.
